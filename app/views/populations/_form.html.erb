<%= form_with(model: population, local: true) do |form| %>
  <% if population.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(population.errors.count, "error") %> prohibited this population from being saved:</h2>

      <ul>
      <% population.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :type_population %>
    <%= form.text_field :type_population, id: :population_type_population %>
  </div>

  <div class="field">
    <%= form.label :age_init %>
    <%= form.number_field :age_init, id: :population_age_init %>
  </div>

  <div class="field">
    <%= form.label :age_end %>
    <%= form.number_field :age_end, id: :population_age_end %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
